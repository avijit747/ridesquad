{
  "name": "ridesquad-server",
  "version": "1.0.0",
  "private": true,
  "description": "RideSquad WebSocket relay + static file server (deployable standalone).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
